import type { NextPage } from "next";
import { Availability } from "alza-components";

const Page: NextPage = () => {
  return (
    <div>
      <h1>Web</h1>
      <Availability />
    </div>
  );
};

export default Page;
